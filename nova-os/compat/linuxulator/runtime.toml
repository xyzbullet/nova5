[runtime]
name = "nova-linux-base"
status = "scaffold"
loader = "freebsd-linuxulator"
architecture = "amd64"

[milestones.static_hello]
description = "Execute a statically linked Linux ELF binary."
status = "not-run"

[milestones.dynamic_cli]
description = "Execute a dynamically linked CLI binary."
status = "not-run"

[milestones.gui_toolkit]
description = "Launch a GTK or equivalent Linux GUI smoke test."
status = "not-run"